{
  "name": "berlin-station-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Berlin transit live monitor with a cached BVG API proxy.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check js/main.js && node --check js/utils.js && node --check js/constants.js"
  },
  "engines": {
    "node": ">=18"
  }
}
